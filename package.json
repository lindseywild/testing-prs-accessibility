{
  "name": "testing-prs-accessibility",
  "homepage": "https://lindseywild.github.io/testing-prs-accessibility",
  "version": "0.1.65",
  "dependencies": {
    "gh-pages": "^4.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "web-vitals": "^3.0.3"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "bun dev --hot",
    "build": "react-scripts build"
  },
  "devDependencies": {
    "react-refresh": "0.10.0",
    "tailwindcss": "^3.2.4",
    "typescript": "latest"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
